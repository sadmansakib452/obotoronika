---
description: Start Nuxt dev server for web-obotoronika
---

Run the development server for the main project. Change to the web-obotoronika directory and start the dev server. Run `cd web-obotoronika && pnpm run dev` and inform the user the server will start at http://localhost:3000.
