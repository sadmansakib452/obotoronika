---
description: Build web-obotoronika for production (nuxt build)
---

Run the production build for the main project. Change to web-obotoronika directory and run `cd web-obotoronika && pnpm run build`. Report any errors.
